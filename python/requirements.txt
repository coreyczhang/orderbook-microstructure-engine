# Analysis layer dependencies.
# generate_synthetic.py and validate.py use only the standard library; these are
# for the OFI backtest (backtest.py) and plotting (plots.py). The OLS regression
# is implemented directly on numpy, so no heavyweight stats package is required.
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
